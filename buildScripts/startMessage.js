import chalk from 'chalk';  // es6 style, used to test if babel transpiling correctly
//chalk allows you to specify colour of output to the console.
console.log(chalk.green("Starting app in dev mode...")); // eslint-disable-line no-console
